buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // NE PAS mettre com.android.tools.build:gradle manuellement
        // Flutter gère cela automatiquement via dev.flutter.flutter-gradle-plugin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"

subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}

subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
